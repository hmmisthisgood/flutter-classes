import 'package:flutter/material.dart';

class ThisIsStateLess extends StatelessWidget {
  const ThisIsStateLess({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
